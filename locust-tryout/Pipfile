[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
fabric3 = "*"
locust = "*"

[scripts]
test = "pipenv run locust"
gitSync = "git fetch --all && git pull"
gitUpdate = "echo hello && echo world"